# Процедурная генерация города с улицами под углами 90° / 45°

Документ описывает модификацию существующей методики процедурной генерации уличной сети таким образом, чтобы **принцип формирования города сохранялся** (cost field, оптимальные пути, рост, иерархия), но **геометрия улиц ограничивалась углами 90° и 45°**.

---

## 0. Базовая идея

Вместо изменения архитектуры алгоритма вводится **ограничение на допустимые направления движения** и соответствующая модификация функции стоимости.

- Город по-прежнему формируется как граф дорог.
- Пути по-прежнему оптимизируют интеграл стоимости.
- Отличие: **дороги состоят из ортогональных и диагональных сегментов**.

---

## 1. Множество допустимых направлений

Вводится дискретное множество направлений:

\[
\Theta=\left\{
0,\;\frac{\pi}{4},\;\frac{\pi}{2},\;\frac{3\pi}{4},\;
\pi,\;\frac{5\pi}{4},\;\frac{3\pi}{2},\;\frac{7\pi}{4}
\right\}
\]

В векторной форме:

\[
D=\{(\pm1,0),(0,\pm1),(\pm\tfrac{1}{\sqrt2},\pm\tfrac{1}{\sqrt2})\}
\]

**Все этапы генерации используют только направления из \(D\).**

---

## 2. Поле стоимости с ортогонально-диагональной анизотропией

### 2.1 Базовая функция стоимости

\[
c(x,y,\theta)=
w_d
+w_s\,\phi(\|\nabla h(x,y)\|)
+w_o\,O(x,y)
-w_a\,A(x,y)
+w_g\,\psi_{grid}(\theta)
\]

Где \(\psi_{grid}\) — штраф за отклонение от разрешённых направлений.

---

### 2.2 Штраф за отклонение от сетки

\[
\psi_{grid}(\theta)=
\min_{\theta_i\in\Theta}
\left|\!\left|\theta-\theta_i\right|\!\right|^2
\]

При большом \(w_g\) оптимальные пути автоматически состоят из сегментов под углами 90° и 45°.

---

## 3. Поиск оптимальных путей

### 3.1 Дискретизация пространства

Используется **8-связная решётка**:

- 4 ортогональных перехода  
- 4 диагональных перехода  

Стоимость шага:

- ортогональный: \(1\)
- диагональный: \(\sqrt{2}\)

---

### 3.2 Стоимость ребра

\[
\mathrm{cost}(u\to v)=
\|\Delta\| \cdot
c\left(\frac{u+v}{2},\theta_{uv}\right)
\]

Поиск минимума — стандартный Dijkstra или A\*.

---

## 4. Рост улиц (local growth)

### 4.1 Выбор направления роста

Вместо непрерывной оптимизации:

\[
d_{k+1}=\arg\min_{d\in D}
\Big(
\alpha\,C(x_k,d)
+\beta\,\|d-d_k\|^2
+\gamma\,\mathrm{Repel}(x_k,d)
\Big)
\]

Где:

- \(C(x_k,d)\) — локальная стоимость шага;
- \(\|d-d_k\|^2\) — штраф за поворот;
- \(\mathrm{Repel}\) — отталкивание от препятствий и существующих дорог.

---

### 4.2 Обновление позиции

\[
x_{k+1}=x_k+\Delta s\cdot d_{k+1}
\]

---

## 5. Ограничение углов поворота

Для предотвращения запрещённых углов вводится штраф:

### Жёсткое ограничение
\[
\Delta\theta \in \{0,\tfrac{\pi}{4},\tfrac{\pi}{2}\}
\]

### Мягкое ограничение
\[
\mathrm{TurnPenalty}(\Delta\theta)=
w_t\cdot
\min_{k\in\mathbb{Z}}
(\Delta\theta-k\tfrac{\pi}{4})^2
\]

---

## 6. Перекрёстки

Перекрёстки классифицируются по углам примыкания:

- T-образные: 0° / 90°
- X-образные: 0° / 90°
- Диагональные: 45° / 135°
- Площадные (редко): 8 направлений

Это упрощает snap, визуализацию и генерацию кварталов.

---

## 7. Геометрия дорог

### 7.1 Представление

- Геометрия дорог — **полилинии**.
- Каждый сегмент прямой.
- Углы между сегментами ∈ {45°, 90°}.

---

### 7.2 Удаление микро-зигзагов

Вместо сплайнов используется правило:

> Если последовательность сегментов даёт поворот +45°, затем −45°,  
> она схлопывается в один прямой сегмент.

---

## 8. Кварталы и вторичная сеть

### 8.1 Тип кварталов

Кварталы образуют:

- прямоугольники,
- октагоны,
- ортогонально-диагональные полигоны.

Узкие и дегенеративные полигоны отфильтровываются.

---

### 8.2 Внутренняя сетка

Внутри квартала:

- линии с ориентацией \(0\) и \(\frac{\pi}{2}\);
- при необходимости — дополнительные линии \(\frac{\pi}{4}\).

---

## 9. Что не требуется менять

- Иерархию сети (артерии → коллекторы → локальные)
- MST и критерии связности
- Поля притяжения и плотности
- Snap, планарность, разрезание рёбер
- Метрики доступности и обходности

---

## 10. Итог

Модификация сводится к **ограничению пространства допустимых направлений** и корректировке функции стоимости.

Это даёт:

- чёткую сетку 90° / 45°,
- сохранение глобальной логики формирования города,
- предсказуемые кварталы и перекрёстки,
- устойчивую процедурную генерацию без криволинейных артефактов.

---
